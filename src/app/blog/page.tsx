import type { Metadata } from "next";
import Link from "next/link";
import { ArrowUpRight, Clock } from "lucide-react";
import { getPublishedPosts } from "@/lib/content";

export const metadata: Metadata = { title: "Blog", description: "Notes on programming, cybersecurity, Linux, networking, and backend engineering." };
export default async function BlogPage() { const posts = await getPublishedPosts(); return <main className="section" style={{ paddingTop: 140, minHeight: "90vh" }}><div className="container"><span className="eyebrow">Field notes</span><h1 className="headline">Writing about software,<br/><span className="gradient-text">security, and systems.</span></h1><div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fit,minmax(280px,1fr))", gap: 16, marginTop: 45 }}>{posts.map((post, index) => <Link href={`/blog/${post.slug}`} key={post.slug} className="card" style={{ padding: 24, textDecoration: "none", color: "inherit", minHeight: 260, display: "flex", flexDirection: "column" }}><div style={{ display: "flex", justifyContent: "space-between" }}><span className="eyebrow">{String(index + 1).padStart(2, "0")} / {post.category}</span><ArrowUpRight size={18} color="var(--muted)"/></div><h2 style={{ fontSize: "1.45rem", lineHeight: 1.25, letterSpacing: "-.03em", margin: "28px 0 12px" }}>{post.title}</h2><p className="muted" style={{ fontSize: 14, lineHeight: 1.65 }}>{post.excerpt}</p><div className="muted" style={{ display: "flex", alignItems: "center", gap: 6, fontSize: 12, marginTop: "auto" }}><Clock size={13}/> {Math.max(2, Math.ceil(post.content.split(/\s+/).length / 40))} min read · {new Date(post.publishedAt).toLocaleDateString("id-ID", { day: "numeric", month: "short", year: "numeric" })}</div></Link>)}</div></div></main>; }
