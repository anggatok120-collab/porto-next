"use client"; import { useState } from "react"; import { signIn } from "next-auth/react"; import { useRouter } from "next/navigation"; import { LockKeyhole } from "lucide-react";
export function LoginForm(){const [error,setError]=useState("");const [busy,setBusy]=useState(false);const router=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const f=new FormData(e.currentTarget);const result=await signIn("credentials",{email:f.get("email"),password:f.get("password"),redirect:false});setBusy(false);if(result?.error)setError("Email atau password tidak valid.");else{router.push("/admin");router.refresh()}}return <form onSubmit={submit} style={{display:"grid",gap:14,marginTop:26}}><label><span style={{fontSize:12}}>Email</span><input className="input" type="email" name="email" required autoComplete="email" style={{marginTop:6}}/></label><label><span style={{fontSize:12}}>Password</span><input className="input" type="password" name="password" required minLength={8} autoComplete="current-password" style={{marginTop:6}}/></label>{error&&<p role="alert" style={{color:"#ff6b6b",fontSize:13}}>{error}</p>}<button className="button button-primary" disabled={busy}><LockKeyhole size={15}/>{busy?"Verifying...":"Sign in securely"}</button></form>}
