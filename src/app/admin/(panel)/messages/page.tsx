import { db } from "@/lib/db"; import { updateMessage } from "../../actions";
export default async function Page(){const rows=await db.contactMessage.findMany({orderBy:{createdAt:"desc"}}).catch(()=>[]);return <><span className="eyebrow">Inbox</span><h1 className="section-title">Contact messages</h1><div style={{display:"grid",gap:12}}>{rows.length?rows.map(x=><article className="card" style={{padding:20}} key={x.id}><div style={{display:"flex",justifyContent:"space-between",gap:12,flexWrap:"wrap"}}><div><b>{x.subject}</b><div className="muted" style={{fontSize:12}}>{x.name} · {x.email} · {x.createdAt.toLocaleString("id-ID")}</div></div><form action={updateMessage} style={{display:"flex",gap:8}}><input type="hidden" name="id" value={x.id}/><select className="input" name="status" defaultValue={x.status}><option>UNREAD</option><option>READ</option><option>REPLIED</option></select><button className="button">Update</button></form></div><p className="muted" style={{lineHeight:1.7}}>{x.message}</p></article>):<div className="card" style={{padding:24}}><p className="muted">Your inbox is clear.</p></div>}</div></>}
