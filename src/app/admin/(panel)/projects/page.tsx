import { db } from "@/lib/db"; import { saveProject,deleteProject } from "../../actions";
export default async function AdminProjects(){const projects=await db.project.findMany({orderBy:{updatedAt:"desc"}}).catch(()=>[]);return <><span className="eyebrow">Content / Projects</span><h1 className="section-title">Project management</h1><div style={{display:"grid",gridTemplateColumns:"minmax(300px,.8fr) minmax(0,1.2fr)",gap:18,alignItems:"start"}}><form action={saveProject} className="card" style={{padding:20,display:"grid",gap:10}}><b>Create or update</b>{[["title","Project title"],["slug","project-slug"],["category","Category"],["description","Short description"],["technologies","Next.js, PostgreSQL"],["githubUrl","GitHub URL"],["liveUrl","Live URL"]].map(([name,placeholder])=><input key={name} className="input" name={name} placeholder={placeholder} required={!name.includes("Url")}/>) }<textarea className="input" name="content" placeholder="Detailed case study" rows={6} required/><label><input type="checkbox" name="featured"/> Featured</label><label><input type="checkbox" name="published"/> Published</label><button className="button button-primary">Save project</button></form><div className="card" style={{padding:20}}><b>All projects</b>{projects.length?projects.map(p=><div key={p.id} style={{display:"flex",justifyContent:"space-between",alignItems:"center",gap:12,padding:"16px 0",borderBottom:"1px solid var(--line)"}}><div><b style={{fontSize:13}}>{p.title}</b><div className="muted" style={{fontSize:12}}>{p.category} · {p.published?"Published":"Draft"}</div></div><form action={deleteProject}><input type="hidden" name="id" value={p.id}/><button className="button">Delete</button></form></div>):<p className="muted">Connect PostgreSQL and run the seed to manage projects.</p>}</div></div><style>{`@media(max-width:950px){main>div:has(>form){grid-template-columns:1fr!important}}`}</style></>}
