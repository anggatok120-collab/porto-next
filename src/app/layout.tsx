import type { Metadata, Viewport } from "next"; import Script from "next/script"; import { Navbar } from "@/components/navbar"; import { Providers } from "@/components/providers"; import { siteConfig } from "@/config/site"; import "./globals.css";
export const metadata:Metadata={metadataBase:new URL(siteConfig.url),title:{default:siteConfig.title,template:"%s · Angga"},description:siteConfig.bio,alternates:{canonical:"/"},openGraph:{type:"website",title:siteConfig.title,description:siteConfig.tagline,url:siteConfig.url,siteName:"Angga Portfolio"},twitter:{card:"summary_large_image",title:siteConfig.title,description:siteConfig.tagline}};
export const viewport:Viewport={width:"device-width",initialScale:1,themeColor:"#05080e"};
export default function RootLayout({children}:{children:React.ReactNode}){const jsonLd={"@context":"https://schema.org","@type":"Person",name:siteConfig.name,jobTitle:siteConfig.role,address:{"@type":"PostalAddress",addressCountry:"Indonesia"},url:siteConfig.url,sameAs:[siteConfig.github,siteConfig.linkedin]};return <html lang="id" suppressHydrationWarning><body><Providers><Navbar/>{children}<footer style={{borderTop:"1px solid var(--line)",padding:"34px 0"}}><div className="container" style={{display:"flex",justifyContent:"space-between",gap:20,flexWrap:"wrap",fontSize:12,color:"var(--muted)"}}><span>© {new Date().getFullYear()} Angga. Built with intention.</span><span>Software · Security · Systems</span></div></footer></Providers><Script id="person-schema" type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(jsonLd)}}/></body></html>}
