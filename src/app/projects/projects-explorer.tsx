"use client"; import { useMemo,useState } from "react"; import { Search } from "lucide-react"; import { ProjectCard,type ProjectLike } from "@/components/project-card";
export function ProjectsExplorer({projects}:{projects:ProjectLike[]}){const [q,setQ]=useState("");const [cat,setCat]=useState("All");const cats=["All",...new Set(projects.map(p=>p.category))];const result=useMemo(()=>projects.filter(p=>(cat==="All"||p.category===cat)&&`${p.title} ${p.description} ${p.technologies.join(" ")}`.toLowerCase().includes(q.toLowerCase())),[projects,q,cat]);return <div style={{marginTop:45}}><div style={{display:"flex",gap:10,flexWrap:"wrap",marginBottom:24}}><label style={{position:"relative",flex:"1 1 240px"}}><Search size={16} style={{position:"absolute",left:14,top:14,color:"var(--muted)"}}/><span className="sr-only">Search projects</span><input className="input" value={q} onChange={e=>setQ(e.target.value)} placeholder="Search projects..." style={{paddingLeft:42}}/></label>{cats.map(x=><button key={x} className="button" onClick={()=>setCat(x)} style={cat===x?{borderColor:"var(--cyan)",color:"var(--cyan)"}:{}}>{x}</button>)}</div><div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(280px,1fr))",gap:16}}>{result.map(p=><ProjectCard key={p.slug} project={p}/>)}</div>{!result.length&&<p className="muted" style={{padding:40,textAlign:"center"}}>No matching project found.</p>}</div>}
