import type { Metadata } from "next"; import { ProjectsExplorer } from "./projects-explorer"; import { getPublishedProjects } from "@/lib/content";
export const metadata:Metadata={title:"Projects",description:"Selected software development and cybersecurity projects by Angga."};
export default async function ProjectsPage(){const projects=await getPublishedProjects();return <main className="section" style={{paddingTop:140,minHeight:"90vh"}}><div className="container"><span className="eyebrow">Project archive</span><h1 className="headline">Systems, tools, and<br/><span className="gradient-text">digital experiments.</span></h1><p className="muted" style={{maxWidth:620,lineHeight:1.7}}>A collection of work spanning full-stack products, backend engineering, automation, networking, and application security.</p><ProjectsExplorer projects={projects}/></div></main>}
