import { deleteContent } from "@/app/admin/actions";
export function AdminContentList({items,model}:{items:Array<{id:string;title:string;meta:string}>,model:"skill"|"experience"|"certificate"|"blogPost"}){return <div className="card" style={{padding:20}}><b>Existing content</b>{items.length?items.map(x=><div key={x.id} style={{display:"flex",justifyContent:"space-between",alignItems:"center",gap:12,padding:"15px 0",borderBottom:"1px solid var(--line)"}}><div><b style={{fontSize:13}}>{x.title}</b><div className="muted" style={{fontSize:12}}>{x.meta}</div></div><form action={deleteContent}><input type="hidden" name="id" value={x.id}/><input type="hidden" name="model" value={model}/><button className="button">Delete</button></form></div>):<p className="muted">No database records yet.</p>}</div>}
