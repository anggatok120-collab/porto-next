"use client"; import { useState } from "react"; import { Send } from "lucide-react"; import { toast } from "sonner";
export function ContactForm(){const [busy,setBusy]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);const form=new FormData(e.currentTarget);const body=Object.fromEntries(form);try{const res=await fetch("/api/contact",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)});const json=await res.json();if(!res.ok)throw new Error(json.message);toast.success("Pesan terkirim. Terima kasih!");(e.target as HTMLFormElement).reset()}catch(err){toast.error(err instanceof Error?err.message:"Gagal mengirim pesan") }finally{setBusy(false)}}return <form onSubmit={submit} className="glass" style={{padding:24,borderRadius:20,display:"grid",gap:14}}><div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(180px,1fr))",gap:14}}><label><span className="sr-only">Nama</span><input className="input" name="name" placeholder="Your name" required minLength={2}/></label><label><span className="sr-only">Email</span><input className="input" name="email" type="email" placeholder="Email address" required/></label></div><label><span className="sr-only">Subjek</span><input className="input" name="subject" placeholder="What can we build together?" required minLength={3}/></label><label><span className="sr-only">Pesan</span><textarea className="input" name="message" placeholder="Tell me about your idea..." required minLength={10} rows={6}/></label><input type="text" name="website" tabIndex={-1} autoComplete="off" className="sr-only"/><button className="button button-primary" disabled={busy}>{busy?"Sending...":<><Send size={16}/>Send message</>}</button></form>}
